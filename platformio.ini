[platformio]
default_envs = esp32dev

[env:esp32dev]
platform = espressif32
board = esp32-s3-devkitc-1

framework = arduino

build_src_filter =
    +<*>

build_flags =
    -DARDUINO_ARCH_ESP32

lib_deps = 
    ; No external libraries needed
